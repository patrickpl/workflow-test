name: Signal Team Notifications

on:
  push:
    branches: [ "main", "master" ]
  issues:
    types: [opened, edited, labeled, unlabeled, assigned, unassigned, milestoned, demilestoned, closed, reopened]
  # Uncomment and customize the workflow_run trigger below to monitor specific workflows
  # Note: workflow_run only works on the default branch (main/master)
  # Replace the workflow names with your actual workflow names
  # workflow_run:
  #   workflows: ["CI", "Tests", "Deploy"]  # List your workflow names here
  #   types: [completed]

jobs:
  signal-notify:
    runs-on: ubuntu-latest
    steps:
      - name: Prepare Notification Data
        id: prep
        shell: bash
        run: |
          REPO_URL="https://github.com/${{ github.repository }}"

          if [ "${{ github.event_name }}" = "push" ]; then
            COMMIT_SHA="${{ github.sha }}"
            SHORT_SHA="$(echo "$COMMIT_SHA" | cut -c1-7)"
            COMMIT_URL="$REPO_URL/commit/$COMMIT_SHA"

            echo "msg<<EOF" >> $GITHUB_OUTPUT
            echo "🚀 New Commit!" >> $GITHUB_OUTPUT
            echo "" >> $GITHUB_OUTPUT
            echo "👤 Who: ${{ github.actor }}" >> $GITHUB_OUTPUT
            echo "📝 What: ${{ github.event.head_commit.message }}" >> $GITHUB_OUTPUT
            echo "🔗 Link: $COMMIT_URL ($SHORT_SHA)" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT

          elif [ "${{ github.event_name }}" = "issues" ]; then
            ISSUE_URL="$REPO_URL/issues/${{ github.event.issue.number }}"

            if [ "${{ github.event.action }}" = "opened" ]; then
              echo "msg<<EOF" >> $GITHUB_OUTPUT
              echo "🆕 New Issue!" >> $GITHUB_OUTPUT
              echo "" >> $GITHUB_OUTPUT
              echo "📌 Title: ${{ github.event.issue.title }}" >> $GITHUB_OUTPUT
              echo "👤 Von: ${{ github.event.issue.user.login }}" >> $GITHUB_OUTPUT
              echo "🔗 Link: $ISSUE_URL" >> $GITHUB_OUTPUT
              echo "EOF" >> $GITHUB_OUTPUT
            elif [ "${{ github.event.action }}" = "closed" ]; then
              echo "msg<<EOF" >> $GITHUB_OUTPUT
              echo "✅ Issue Solved!" >> $GITHUB_OUTPUT
              echo "" >> $GITHUB_OUTPUT
              echo "📌 Title: ${{ github.event.issue.title }}" >> $GITHUB_OUTPUT
              echo "👤 Solved by: ${{ github.event.sender.login }}" >> $GITHUB_OUTPUT
              echo "🔗 Link: $ISSUE_URL" >> $GITHUB_OUTPUT
              echo "EOF" >> $GITHUB_OUTPUT
            else
              echo "msg<<EOF" >> $GITHUB_OUTPUT
              echo "🔄 Issue Updated!" >> $GITHUB_OUTPUT
              echo "" >> $GITHUB_OUTPUT
              echo "📌 Title: ${{ github.event.issue.title }}" >> $GITHUB_OUTPUT
              echo "🔧 Action: ${{ github.event.action }}" >> $GITHUB_OUTPUT
              echo "👤 Updated by: ${{ github.event.sender.login }}" >> $GITHUB_OUTPUT
              echo "🔗 Link: $ISSUE_URL" >> $GITHUB_OUTPUT
              echo "EOF" >> $GITHUB_OUTPUT
            fi

          elif [ "${{ github.event_name }}" = "workflow_run" ]; then
            WORKFLOW_URL="$REPO_URL/actions/runs/${{ github.event.workflow_run.id }}"
            WORKFLOW_NAME="${{ github.event.workflow_run.name }}"
            CONCLUSION="${{ github.event.workflow_run.conclusion }}"

            if [ "$CONCLUSION" = "success" ]; then
              echo "msg<<EOF" >> $GITHUB_OUTPUT
              echo "✅ CI/CD Success!" >> $GITHUB_OUTPUT
              echo "" >> $GITHUB_OUTPUT
              echo "📦 Workflow: $WORKFLOW_NAME" >> $GITHUB_OUTPUT
              echo "👤 Triggered by: ${{ github.event.workflow_run.triggering_actor.login }}" >> $GITHUB_OUTPUT
              echo "🔗 Link: $WORKFLOW_URL" >> $GITHUB_OUTPUT
              echo "EOF" >> $GITHUB_OUTPUT
            elif [ "$CONCLUSION" = "failure" ]; then
              echo "msg<<EOF" >> $GITHUB_OUTPUT
              echo "❌ CI/CD Failed!" >> $GITHUB_OUTPUT
              echo "" >> $GITHUB_OUTPUT
              echo "📦 Workflow: $WORKFLOW_NAME" >> $GITHUB_OUTPUT
              echo "👤 Triggered by: ${{ github.event.workflow_run.triggering_actor.login }}" >> $GITHUB_OUTPUT
              echo "🔗 Link: $WORKFLOW_URL" >> $GITHUB_OUTPUT
              echo "⚠️ Action Required: Check the workflow logs for details" >> $GITHUB_OUTPUT
              echo "EOF" >> $GITHUB_OUTPUT
            elif [ "$CONCLUSION" = "cancelled" ]; then
              echo "msg<<EOF" >> $GITHUB_OUTPUT
              echo "🚫 CI/CD Cancelled!" >> $GITHUB_OUTPUT
              echo "" >> $GITHUB_OUTPUT
              echo "📦 Workflow: $WORKFLOW_NAME" >> $GITHUB_OUTPUT
              echo "👤 Triggered by: ${{ github.event.workflow_run.triggering_actor.login }}" >> $GITHUB_OUTPUT
              echo "🔗 Link: $WORKFLOW_URL" >> $GITHUB_OUTPUT
              echo "EOF" >> $GITHUB_OUTPUT
            fi

          else
            echo "Unsupported event type: ${{ github.event_name }}"
          fi

