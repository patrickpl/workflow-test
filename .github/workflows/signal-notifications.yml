name: Signal Team Notifications

on:
  push:
    branches: [ "main", "master" ]
  issues:
    types: [opened]

jobs:
  signal-notify:
    runs-on: ubuntu-latest
    steps:
      - name: Prepare Notification Data
        id: prep
        shell: bash
        run: |
          if [ "${{ github.event_name }}" == "push" ]; then
            echo "msg<<EOF" >> $GITHUB_OUTPUT
            echo "🚀 New Commit!" >> $GITHUB_OUTPUT
            echo "" >> $GITHUB_OUTPUT
            echo "👤 Who: ${{ github.actor }}" >> $GITHUB_OUTPUT
            echo "📝 What: ${{ github.event.head_commit.message }}" >> $GITHUB_OUTPUT
            echo "🔗 Link: ${{ github.event.head_commit.url }}" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          elsif [ "${{ github.event_name }}" == "issues" ]; then
            echo "msg<<EOF" >> $GITHUB_OUTPUT
            echo "👋 INPUT NEEDED!" >> $GITHUB_OUTPUT
            echo "" >> $GITHUB_OUTPUT
            echo "📌 Titel: ${{ github.event.issue.title }}" >> $GITHUB_OUTPUT
            echo "👤 Von: ${{ github.event.issue.user.login }}" >> $GITHUB_OUTPUT
            echo "🔗 Link: ${{ github.event.issue.html_url }}" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          else
            echo ${{github.event_name}}
          fi

      - name: Download Avatar and Convert to Base64
        run: |
          curl -L -o avatar.png "${{ steps.prep.outputs.avatar }}"
          echo "AVATAR_B64=$(base64 -w 0 avatar.png)" >> $GITHUB_ENV

      - name: Send to Signal
        env:
          # Wir laden die Nachricht in eine Umgebungsvariable, um JSON-escaping Probleme zu vermeiden
          FINAL_MSG: ${{ steps.prep.outputs.msg }}
        run: |
          # jq ist auf GitHub-Runnern vorinstalliert und hilft uns, 
          # ein valides JSON zu bauen, selbst wenn der Commit-Text Anführungszeichen enthält.
          JSON_BODY=$(jq -n \
            --arg msg "$FINAL_MSG" \
            --arg num "${{ secrets.SIGNAL_SENDER_NUMBER }}" \
            --arg rec "${{ secrets.SIGNAL_GROUP_ID }}" \
            --arg img "${{ env.AVATAR_B64 }}" \
            '{
              message: $msg,
              number: $num,
              recipients: [$rec],
              base64_attachments: [$img],
              text_mode: "styled"
            }')

          curl -X POST "${{ secrets.SIGNAL_API_URL }}/v1/send" \
          -H "Content-Type: application/json" \
          -d "$JSON_BODY"
